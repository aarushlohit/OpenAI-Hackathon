# Prompt Packs

Prompt packs will be introduced in Phase 2 when domain agents begin calling gateway-backed analysis services.

Rules:

- Prompts must describe untrusted input boundaries.
- Prompts must request schema-compatible output.
- Prompts must not contain secrets.
- Provider-specific prompt tuning belongs behind gateway-aware services, not inside agents.

