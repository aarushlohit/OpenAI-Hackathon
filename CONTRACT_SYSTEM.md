# Contract System

Agent contracts are declared in `AGENT_CONTRACT.yaml`.

The contract system formalizes:

- agent IO boundaries.
- event permissions.
- workflow eligibility.
- risk and confidence ceilings.
- allowed actions.

Contract validation is intentionally separate from agent code so governance can evolve without rewriting agents.

