name: hermes_x
description: Hermes-X — Recruitment Scam Investigator (WebView wrapper)
publish_to: "none"
version: 2.0.0

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  webview_flutter: ^4.10.0

flutter:
  uses-material-design: true
