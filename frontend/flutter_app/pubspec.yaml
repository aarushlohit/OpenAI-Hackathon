name: detective_hermes_agent
description: Detective Hermes Agent — Flutter WebView wrapper for the deployed investigation workspace.
publish_to: "none"
version: 2.0.0

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  webview_flutter: ^4.10.0

flutter:
  uses-material-design: true
