# Detective Hermes Agent Flutter Wrapper

This Flutter app intentionally contains only a WebView shell.

It loads the deployed web workspace:

https://detective-hermes-agent.vercel.app

The native Flutter UI was removed so the mobile/desktop app always mirrors the production Web UI. A branded lazy loading screen is shown until the WebView finishes loading.
