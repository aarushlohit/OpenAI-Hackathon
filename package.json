{
  "name": "detective-hermes-agent",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "cd frontend/react_app && npm install && npm run build",
    "dev": "cd frontend/react_app && npm run dev"
  }
}
